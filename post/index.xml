<?xml version="1.0" encoding="utf-8" standalone="yes" ?>
<rss version="2.0" xmlns:atom="http://www.w3.org/2005/Atom">
  <channel>
    <title>Post-rsses on Notes on #Golang</title>
    <link>https://voigt.github.io/post/index.xml</link>
    <description>Recent content in Post-rsses on Notes on #Golang</description>
    <generator>Hugo -- gohugo.io</generator>
    <language>en-us</language>
    <lastBuildDate>Thu, 20 Oct 2016 20:37:05 +0200</lastBuildDate>
    <atom:link href="https://voigt.github.io/post/index.xml" rel="self" type="application/rss+xml" />
    
    <item>
      <title>How to handle JSON configs in Go</title>
      <link>https://voigt.github.io/post/handle_json_config_in_go/</link>
      <pubDate>Thu, 20 Oct 2016 20:37:05 +0200</pubDate>
      
      <guid>https://voigt.github.io/post/handle_json_config_in_go/</guid>
      <description>

&lt;p&gt;At a certain point of development it becomes good practice to decouple the config from the code. Since I&amp;rsquo;m coming from a Node.js background, I&amp;rsquo;m used to use JSON for configuration. Luckily the Go standard library already comes with JSON support.&lt;/p&gt;

&lt;p&gt;The benefits of JSON are that it is fairly simple to parse and human readable/editable while offering semantics for lists and mappings (which can become quite handy), which is not the case with many ini-type config parsers.&lt;/p&gt;

&lt;h4 id=&#34;example-usage&#34;&gt;Example Usage&lt;/h4&gt;

&lt;p&gt;Lets assume we have following JSON config:&lt;/p&gt;

&lt;p&gt;&lt;strong&gt;config.json&lt;/strong&gt;&lt;/p&gt;

&lt;pre&gt;&lt;code&gt;{
    &amp;quot;host&amp;quot;: &amp;quot;http://host.name&amp;quot;,
    &amp;quot;path&amp;quot;: &amp;quot;/api/v1/you/know/the/route/&amp;quot;,
    &amp;quot;apikey&amp;quot;: &amp;quot;abcdefg&amp;quot;
}
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;Reading this config can look like this:&lt;/p&gt;

&lt;p&gt;&lt;strong&gt;main.go&lt;/strong&gt;&lt;/p&gt;

&lt;pre&gt;&lt;code class=&#34;language-go&#34;&gt;package main

import (
	&amp;quot;fmt&amp;quot;
	&amp;quot;encoding/json&amp;quot;
	&amp;quot;os&amp;quot;
)

// Create a configuration struct
type Configuration struct {
	Host   string `json:&amp;quot;host&amp;quot;`
	Path   string `json:&amp;quot;path&amp;quot;`
	Apikey string `json:&amp;quot;apikey&amp;quot;`
}

var Urlpath string

func main() {

	// Open config file
	configFile, _ := os.Open(&amp;quot;config.json&amp;quot;)

	decoder := json.NewDecoder(configFile) 	// Initialize a JSON decoder
	c := Configuration{}                    //
	err := decoder.Decode(&amp;amp;c)               // Decode JSON and save it in c

	if err != nil {
		fmt.Println(&amp;quot;Could not Read Configuration&amp;quot;, err)
		return
	}

	Urlpath = c.Host + c.Path
	fmt.Println(Urlpath)

}
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;So running our program would output the following:&lt;/p&gt;

&lt;pre&gt;&lt;code&gt;$ go run main.go
http://host.name/api/v1/you/know/the/route/
&lt;/code&gt;&lt;/pre&gt;

&lt;h4 id=&#34;extend-the-configuration-struct&#34;&gt;Extend the &lt;code&gt;Configuration struct&lt;/code&gt;&lt;/h4&gt;

&lt;p&gt;main.go&lt;/p&gt;

&lt;pre&gt;&lt;code class=&#34;language-go&#34;&gt;[...]

type Configuration struct {
	Host   string `json:&amp;quot;host&amp;quot;`
	Path   string `json:&amp;quot;path&amp;quot;`
	Apikey string `json:&amp;quot;apikey&amp;quot;`
}

// this will allow to access the whole URL via c.UrlPath()
func (c *Configuration) UrlPath() string {
	return c.Host + c.Path
}

func (c *Configuration) ApiSuffix() string {
	return &amp;quot;?apikey=&amp;quot; + c.Apikey
}

func main() {

	configFile, _ := os.Open(&amp;quot;config.json&amp;quot;)
	decoder := json.NewDecoder(configFile)

	c := Configuration{}
	err := decoder.Decode(&amp;amp;c)

	if err != nil {
		fmt.Println(&amp;quot;Could not Read Configuration&amp;quot;, err)
		return
	}

	fmt.Println(c.UrlPath() + &amp;quot;someParameter&amp;quot; + c.ApiSuffix())

}
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;Of course, other config formats, such as YAML or TOML would also do their work, but they&amp;rsquo;d need to be added separately. JSON is build in and awesome ;)&lt;/p&gt;
</description>
    </item>
    
  </channel>
</rss>